@AGENTS.md

## Específico do Claude Code

- As skills de Django da Vinta (`django-expert`, `django-safe-migration`,
  `django-reviewer`) são instaladas pelo `.claude/settings.json`. Use
  `django-reviewer` para revisar mudanças antes de abrir um PR.
- Para uma funcionalidade nova, siga a skill do projeto `tdd-feature`.
